package mx.unam.ciencias.myd.proyecto3;
/**
 * <p> Clase para operaciones de algebra lineal.</p>
 * 
 * <p> La clase cuenta con métodos para realizar operaciones de algebra lineal sobre matrices y vectores.</p>
 * <p> Cada metodo tiene su respectiva version en los tipos de datos int, long y double.</p>
 
 */
public class UtilAlgebraLineal  {

    /** Regresa la suma de dos vectores.
     * @param v1 el primer vector.
     * @param v2 el segundo vector.
     * @return la suma de los vectores v1 y v2.
     */
    public static int[] suma(int[] v1, int[] v2){
        // Codigo pendiente
        return null;
    }
    

    /** Regresa el producto de un vector fila por un escalar k.
     * @param m la matriz de la que se quiere calcular el producto.
     * @param v el vector de la que se quiere calcular el producto.
     * @return el producto de la matriz m por el vector v.
     */
    public static int[] producto(int[] v, int k){
        // Codigo pendiente
        return null;
    }

    /** Regresa el producto punto de dos vectores.
     * @param v1 el primer vector.
     * @param v2 el segundo vector.
     * @return el producto punto de los vectores v1 y v2.
     */
    public static int productoPunto(int[] v1, int[] v2){
        // Codigo pendiente
        return 0;
    }

    /** Regresa la matriz que resulta de intercambiar la fila i por la fila j.
     * @param m la matriz de la que se quiere cambiar una fila.
     * @param i la fila que se quiere intercambiar.
     * @param j la fila que se quiere intercambiar.
     * @return el producto de la matriz m por el vector v.
     */
    public static int[][] intercambiaFila(int[][] m, int i, int j){
        // Codigo pendiente
        return null;
    }

    /** Regresa el vector columna tal que AX = B.
     * @param A la matriz de coeficientes.
     * @param B el vector de resultados.
     * @return el vector fila isomorfo al vector columna X tal que AX = B 
     */
    public static int[] solucionSistema(int[][] A, int[] B){
        // Codigo pendiente
        return null;
    }

}